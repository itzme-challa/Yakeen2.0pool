{
  "name": "telegram-bot-vercel-boilerplate",
  "version": "1.1.0",
  "description": "Telegram Bot Vercel Boilerplate",
  "main": "src/index.ts",
  "author": "Mark Pavlov (https://github.com/sollidy)",
  "homepage": "https://github.com/sollidy/telegram-bot-vercel-boilerplate",
  "dependencies": {
    "@vercel/node": "^5.1.16",
    "axios": "^1.7.2",
    "dotenv-cli": "^8.0.0",
    "firebase": "^10.12.2",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/debug": "^4.1.12",
    "@types/node": "^22.8.6",
    "@vercel/ncc": "^0.38.2",
    "debug": "^4.3.7",
    "nodemon": "^3.1.7",
    "prettier": "^3.3.3",
    "ts-node": "^10.9.2",
    "typescript": "^ core/index.ts
import { VercelRequest, VercelResponse } from '@vercel/node';
import { Telegraf } from 'telegraf';

export const development = async (bot: Telegraf) => {
  console.log('Starting bot in development mode...');
  await bot.launch();
  console.log('Bot started!');
};

export const production = async (req: VercelRequest, res: VercelResponse, bot: Telegraf) => {
  try {
    await bot.handleUpdate(req.body);
    res.status(200).send('OK');
  } catch (error) {
    console.error('Error in production handler:', error);
    res.status(500).send('Error');
  }
};
